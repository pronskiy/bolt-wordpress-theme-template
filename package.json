{
  "name": "wordpress-theme-template",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "npx --yes @wp-playground/cli@latest server --port=8080 --skip-browser --skipWordPressSetup --mountBeforeInstall=\"./prebuilt-wp-content-for-wp-6.8/:/wordpress\"",
    "dev_t": "npx http-server -o ./",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/pronskiy/bolt-wordpress-theme-template.git"
  },
  "private": true
}
