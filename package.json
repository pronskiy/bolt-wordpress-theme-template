{
  "name": "wordpress-theme-template",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "npx --yes @wp-playground/cli@latest server --port=80 --skip-browser",
    "dev_t": "npx http-server -o ./",
    "test": "echo \"Error: no test specified\" && exit 1",
    "install": "mkdir -p ~/.wordpress-playground; cp ./dev-dependencies/* ~/.wordpress-playground/"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/pronskiy/bolt-wordpress-theme-template.git"
  },
  "private": true
}
